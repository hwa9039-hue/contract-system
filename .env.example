# ── 로컬 개발 (npm run dev) ─────────────────────────────────────────────
# API 기본값: http://localhost:8000
# NAS API(이미지 포함)를 쓰려면 프로젝트 루트 `.env.local` 에 예:
#   VITE_API_BASE_URL=http://192.168.0.100:8000
# (변경 후 npm run dev 재시작)
#
# ── 운영 빌드 (npm run build) ─────────────────────────────────────────
# 운영 API: public/api-config.js 의 __CMS_API_BASE_URL__
# 백엔드 AUTH_SHARED_PASSWORD와 동일하게 맞추세요.
VITE_APP_SHARED_PASSWORD=smartdi2026!
# 로그인 계정 — 전원 동일 권한. 형식: 비밀번호:표시명,비밀번호:표시명
VITE_APP_ADMIN_ACCOUNTS=hy9039!:정화영,jhjoung!:정주희,kk2331!:전기웅,nov1st!:유영무,sskim!:김성수,yongja_lee!:이용자,pjb9878!:박재범,jslee!:이재승,wizard1221!:전재우,ssj8845!:신상준
# 하위 호환. 새 계정은 VITE_APP_ADMIN_ACCOUNTS 에 넣는다.
VITE_APP_MANAGER_ACCOUNTS=

# (선택) 빌드에 포함 시 콘솔에 API 헬스·로그인 단계 로그. 운영에서는 보통 비우고
# 브라우저에서 ?cmsApiProbe=1 로 켜는 방식을 권장합니다.
# VITE_CMS_API_PROBE=true

# 건축정보 POST 실패 시에만 로컬 저장 폴백(기본 false). 강제 mock: true
# VITE_DISCOVERY_API_MOCK=true

# (선택) MUI X Data Grid Pro 라이선스 키 — 단가관리 Tree Grid
# VITE_MUI_X_LICENSE_KEY=

